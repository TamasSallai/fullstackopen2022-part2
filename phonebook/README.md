# Phonebook

This exercise is for part2 of the fullstackopen2022 course

To run this project:

- Run the command "npm server" to start the json-server included as dev dependencie (https://github.com/typicode/json-server)
- Run the react app with the command "npm start"
